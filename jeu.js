pokeball.addEventListener("click", () => {
  pokeball.classList.toggle("pokeballClick");
  let roll = Math.floor(Math.random() * 151 + 1);

  if (roll === 1) {
    bulbi.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 2) {
    herbi.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 3) {
    flori.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 4) {
    salam.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 5) {
    repti.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 6) {
    dracau.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 7) {
    carap.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 8) {
    carab.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 9) {
    torta.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 10) {
    chenip.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 11) {
    chrys.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 12) {
    papi.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 13) {
    aspic.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 14) {
    cocon.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 15) {
    darda.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 16) {
    roucoul.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 17) {
    roucoups.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 18) {
    rouca.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 19) {
    ratat.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 20) {
    ratac.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 21) {
    piaf.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 22) {
    raps.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 23) {
    abo.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 24) {
    arbo.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 25) {
    pika.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 26) {
    raichu.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 27) {
    sabel.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 28) {
    sablai.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 29) {
    nidofem.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 30) {
    nidorin.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 31) {
    nidoqueen.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 32) {
    nidomal.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 33) {
    nidorino.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 34) {
    nidokin.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 35) {
    melo.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 36) {
    melodel.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 37) {
    goupi.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 38) {
    feuna.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 39) {
    rondou.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 40) {
    grodou.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 41) {
    nosfe.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 42) {
    nosfer.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 43) {
    mysthe.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 44) {
    orti.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 45) {
    rafl.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 46) {
    paras.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 47) {
    pares.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 48) {
    mimi.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 49) {
    aero.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 50) {
    taupi.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 51) {
    trio.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 52) {
    maiou.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 53) {
    persi.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 54) {
    psyko.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 55) {
    akwa.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 56) {
    fero.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 57) {
    colo.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 58) {
    cani.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 59) {
    arca.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 60) {
    ptit.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 61) {
    teta.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 62) {
    tarta.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 63) {
    abra.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 64) {
    kada.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 65) {
    alak.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 66) {
    macho.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 67) {
    machop.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 68) {
    mako.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 69) {
    cheti.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 70) {
    bousti.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 71) {
    empi.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 72) {
    tenta.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 73) {
    tentac.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 74) {
    raca.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 75) {
    grava.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 76) {
    grolem.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 77) {
    pony.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 78) {
    galo.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 79) {
    ramo.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 80) {
    flaga.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 81) {
    magne.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 82) {
    magnon.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 83) {
    canar.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 84) {
    doduo.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 85) {
    dodri.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 86) {
    ota.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 87) {
    laman.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 88) {
    tadm.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 89) {
    grota.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 90) {
    koki.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 91) {
    crusta.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 92) {
    fanto.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 93) {
    spectr.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 94) {
    ecto.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 95) {
    onix.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 96) {
    sopo.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 97) {
    hypno.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 98) {
    krab.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 99) {
    krabo.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 100) {
    volto.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 101) {
    electro.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 102) {
    noeu.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 103) {
    noad.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 104) {
    osse.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 105) {
    ossa.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 106) {
    kick.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 107) {
    tygn.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 108) {
    excel.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 109) {
    smog.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 110) {
    smogro.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 111) {
    rhino.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 112) {
    rhinof.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 113) {
    levei.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 114) {
    sacq.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 115) {
    kangou.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 116) {
    hypo.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 117) {
    hypoc.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 118) {
    poissi.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 119) {
    poisso.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 120) {
    stari.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 121) {
    staross.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 122) {
    mime.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 123) {
    insec.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 124) {
    lipo.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 125) {
    elek.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 126) {
    magma.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 127) {
    scara.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 128) {
    tauro.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 129) {
    magic.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 130) {
    levia.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 131) {
    lock.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 132) {
    metam.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 133) {
    evo.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 134) {
    aqua.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 135) {
    volta.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 136) {
    pyro.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 137) {
    pory.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 138) {
    amon.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 139) {
    amonis.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 140) {
    kabu.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 141) {
    kabut.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 142) {
    ptera.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 143) {
    ronfl.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 144) {
    artik.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 145) {
    electo.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 146) {
    sulfu.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 147) {
    mini.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 148) {
    draco.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 149) {
    draloss.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 150) {
    mewtwo.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
  if (roll === 151) {
    mew.style.zIndex = 100;
    setTimeout(() => {
      let jeu = prompt("Quel est ce Pokémon ?");
      if (jeu === pokelist[roll - 1]) {
        score.textContent = `Bien joué mon chacalou`;
        setTimeout(() => {
          location.reload();
        }, 2000);
      } else {
        location.reload();
      }
    }, 500);
  }
});
